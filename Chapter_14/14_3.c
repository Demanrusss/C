#include <stdio.h>
#include <string.h>

int main(int argc, char *argv[]) {

    if (argc != 3)
        printf("Enter command line (mask is: command input output).\n");
    else {
        printf("\n%s\n", argv);
    }

return 0;
}
